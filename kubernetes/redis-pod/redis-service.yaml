apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  selector:
    run: redis  # 匹配Pod的标签（需根据实际标签调整）
  ports:
    - protocol: TCP
      port: 6379        # Service端口
      targetPort: 6379  # Pod端口（Redis默认端口）
  type: ClusterIP       # 集群内部访问，也可改为NodePort暴露到外部
